#include "A1Z26.h"

string A1Z26_encrypt(const string& text) {
    string encrypted = text;
    return encrypted;
} 

string A1Z26_decrypt(const string& encrypted_text) {
    string decrypted = encrypted_text;
    return decrypted;
}