#include "Bacon.h"

string bacon_encrypt(const string& text) {
    string encrypted = text;
    return encrypted;
} 

string bacon_decrypt(const string& encrypted_text) {
    string decrypted = encrypted_text;
    return decrypted;
}