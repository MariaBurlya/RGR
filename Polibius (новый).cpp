#include "Polibius.h"

string polibius_encrypt(const string& text) {
    string encrypted = text;
    return encrypted;
} 

string polibius_decrypt(const string& encrypted_text) {
    string decrypted = encrypted_text;
    return decrypted;
}